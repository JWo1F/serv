# Pages and addresses

This file is reached at `/guide/pages`, without its extension. Asking for
`/guide/pages.md` should redirect here rather than serve the same page twice.

## Anchors

This heading is why `[a](guide/pages.md#anchors)` in the README has somewhere to
land.

## A folder below

[The nested folder](nested/) has no document in it at all, so it falls back to
serv's own directory listing — worth seeing next to a rendered page.

[Back to the guide](index.md)
